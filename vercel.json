{
  "version": 2,
  "installCommand": "pnpm install",
  "buildCommand": "pnpm run build",
  "functions": {
    "api/webhook.ts": {
      "runtime": "@vercel/node"
    }
  },
  "routes": [
    {
      "src": "/webhook",
      "dest": "/api/webhook"
    }
  ]
}
